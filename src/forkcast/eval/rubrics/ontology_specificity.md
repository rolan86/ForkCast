Rate the domain-specificity of these ontology entity types.

1 = Generic types only (Person, Organization, Location — could apply to any domain)
2 = Mostly generic with 1 domain-specific type
3 = Mix of generic and domain-specific types
4 = Mostly domain-specific types that reflect the platform/topic
5 = All types are domain-native and specific (e.g., Influencer, Brand, Regulator for social media)

## Entity Types
{{ content }}

Return ONLY valid JSON: {"score": N, "justification": "one sentence explanation"}
